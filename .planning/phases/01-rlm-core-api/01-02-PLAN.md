---
phase: 01-rlm-core-api
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/rlm_assistant/rlm_client.py
  - src/rlm_assistant/system_prompt.py
autonomous: true
requirements:
  - RLM ядро с поддержкой DeepSeek API
  - System prompt для dev-задач
must_haves:
  truths:
    - "RLM instance is created with DeepSeek backend using config settings"
    - "System prompt instructs RLM to act as a dev assistant with recursive decomposition"
    - "rlm_client.completion('hello') returns a response string from DeepSeek"
  artifacts:
    - path: "src/rlm_assistant/rlm_client.py"
      provides: "RLM client initialization and completion wrapper"
      contains: "RLM("
    - path: "src/rlm_assistant/system_prompt.py"
      provides: "Dev assistant system prompt for RLM"
      contains: "answer"
  key_links:
    - from: "src/rlm_assistant/rlm_client.py"
      to: "src/rlm_assistant/config.py"
      via: "get_settings().rlm_backend_kwargs"
      pattern: "get_settings.*rlm_backend_kwargs"
    - from: "src/rlm_assistant/rlm_client.py"
      to: "src/rlm_assistant/system_prompt.py"
      via: "import DEV_SYSTEM_PROMPT"
      pattern: "DEV_SYSTEM_PROMPT"
    - from: "src/rlm_assistant/rlm_client.py"
      to: "rlms"
      via: "from rlm import RLM"
      pattern: "from rlm import RLM"
---

<objective>
Create the RLM client wrapper and system prompt.

Purpose: Initialize RLM with DeepSeek as the backend (per CONTEXT.md D-01: use `backend="openai"` with DeepSeek base_url). Provide a clean interface for creating RLM instances and a dev-focused system prompt that teaches RLM to use its recursive capabilities for software engineering tasks.
Output: src/rlm_assistant/rlm_client.py, src/rlm_assistant/system_prompt.py
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-rlm-core-api/01-CONTEXT.md
@.planning/research/rlm-integration.md
@src/rlm_assistant/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create system prompt module</name>
  <files>src/rlm_assistant/system_prompt.py</files>
  <read_first>
    .planning/phases/01-rlm-core-api/01-CONTEXT.md
    .planning/research/rlm-integration.md
  </read_first>
  <action>
    Create src/rlm_assistant/system_prompt.py with a DEV_SYSTEM_PROMPT constant.

    The prompt must instruct the RLM to:
    1. Act as an expert software development assistant
    2. Use llm_query() for quick lookups and rlm_query() for complex sub-tasks requiring deeper reasoning
    3. Decompose complex problems into subtasks and process them recursively
    4. Always provide concrete, actionable code examples
    5. Set answer["content"] with the final result and answer["ready"] = True when done
    6. Use print() to output intermediate results visible in the next iteration

    The prompt should cover these dev domains: code analysis, bug finding, code generation, refactoring, documentation, architecture decisions.

    Reference the RLM REPL functions documented in rlm-integration.md section 3 (llm_query, rlm_query, SHOW_VARS, answer dict).

    Format as a triple-quoted Python string constant. Keep it under 2000 chars — RLM injects its own REPL instructions automatically, so this is additive context only.
  </action>
  <verify>
    <automated>cd /home/ros2/rlmresearch && python -c "from rlm_assistant.system_prompt import DEV_SYSTEM_PROMPT; assert len(DEV_SYSTEM_PROMPT) > 200; assert 'answer' in DEV_SYSTEM_PROMPT; assert 'llm_query' in DEV_SYSTEM_PROMPT"</automated>
  </verify>
  <acceptance_criteria>
    - DEV_SYSTEM_PROMPT is a string constant > 200 chars
    - Mentions answer["content"] and answer["ready"] as the completion mechanism
    - References llm_query and rlm_query as available tools
    - Covers software development use cases (code analysis, bug fixing, code generation, refactoring)
    - Does not duplicate RLM's built-in REPL instructions (those are injected automatically)
  </acceptance_criteria>
  <done>System prompt exists, references RLM REPL functions, covers dev assistant persona</done>
</task>

<task type="auto">
  <name>Task 2: Create RLM client module</name>
  <files>src/rlm_assistant/rlm_client.py</files>
  <read_first>
    .planning/research/rlm-integration.md
    src/rlm_assistant/config.py
    src/rlm_assistant/system_prompt.py
  </read_first>
  <action>
    Create src/rlm_assistant/rlm_client.py with functions to create and manage RLM instances.

    Required functions:

    1. create_rlm(settings=None) -> RLM:
       - Imports RLM from the rlm package
       - Calls get_settings() if settings not provided
       - Creates RLM with:
         backend="openai"
         backend_kwargs=settings.rlm_backend_kwargs (contains api_key, model_name, base_url)
         custom_system_prompt=DEV_SYSTEM_PROMPT
         verbose=settings.rlm_verbose
         persistent=settings.rlm_persistent
         compaction=settings.rlm_compaction
         compaction_threshold_pct=settings.rlm_compaction_threshold
         max_iterations=settings.rlm_max_iterations
       - Returns the RLM instance

    2. chat(message: str, rlm: RLM = None) -> str:
       - Creates RLM via create_rlm() if rlm not provided
       - Calls rlm.completion(message)
       - Returns result.response (the string content)
       - Wraps in try/except, logs errors with logging module

    Module-level singleton pattern: provide a module variable _rlm_instance and get_rlm() that lazily creates it. This avoids recreating the RLM environment per request (important for persistent=True).

    Use logging module (logger = logging.getLogger(__name__)) for debug/error output.
  </action>
  <verify>
    <automated>cd /home/ros2/rlmresearch && DEEPSEEK_API_KEY=test-key python -c "from rlm_assistant.rlm_client import create_rlm, chat; print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - create_rlm() constructs an RLM instance with backend="openai" and DeepSeek backend_kwargs from config
    - DEV_SYSTEM_PROMPT is passed as custom_system_prompt to RLM constructor
    - chat() accepts a message string and returns a response string
    - Module uses logging (not print) for diagnostics
    - Singleton pattern via get_rlm() avoids re-creating RLM per request
    - persistent=True and compaction=True are passed to RLM (per CONTEXT.md decisions)
  </acceptance_criteria>
  <done>RLM client wraps library with DeepSeek backend, system prompt, and singleton pattern</done>
</task>

</tasks>

<verification>
- `python -c "from rlm_assistant.rlm_client import create_rlm; from rlm_assistant.system_prompt import DEV_SYSTEM_PROMPT; print('OK')"` succeeds
- RLM constructor receives backend="openai", correct base_url, and system prompt
</verification>

<success_criteria>
RLM client initializes with DeepSeek backend, uses dev system prompt, provides chat() interface.
</success_criteria>

<output>
After completion, create `.planning/phases/01-rlm-core-api/01-02-SUMMARY.md`
</output>
